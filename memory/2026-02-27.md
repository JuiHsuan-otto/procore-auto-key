# 2026-02-27 記憶日誌

## 已完成任務
1. 修復手機版導航列：加入 WORKS/BLOG/CALL 快捷鍵。
2. 修復 Skoda Kodiaq 圖片路徑問題。
3. 掛載 BMW FEM 技術文章影片 `vid/bmw_tech.mp4`。
4. 替換美製車文章圖片為「野馬 Mustang」現場實拍圖。
5. 發布全新 SEO 權威長文：`article-car-key-lost-rescue-central-taiwan.html`。
6. 修正 SEO 文章 404 問題（已執行 Git Push 推送檔案）。

## 關鍵決策
- 為了提升在地搜尋權重，SEO 文章 Alt 文字設定為「極致核心汽車鑰匙全丟救援-台中彰化」。
- 手機版導航為了直覺性，捨棄漢堡選單，改用直接顯示的文字按鈕。

## 待辦事項
- 監控新發布文章的搜尋排名。
- 檢查 Vercel 佈署是否完全生效。

## 2026-02-27 官網整體稽核、發布修復與救援需求工具
- 依 `AI_CONTENT_AUTOMATION_SOP.md` 完成官網靜態稽核；官網維持唯一 canonical，外部平台僅草稿/導流，GBP API 路徑停用。
- 修復 `article-audi-r8-neihu-all-keys-lost.html` 全頁問號亂碼，重寫為自然、去識別化且不外流技術流程的案例文；聯絡資訊確認為 0909277670 / @420gknem。
- 修正 `article-car-key-lost-rescue-central-taiwan.html` 被跳脫引號破壞的 CTA 連結。
- 強化 `publish_tool.py`：檢查文章/canonical/亂碼/案例圖片、同步首頁最新案例、保留 cases.json 既有 ID，並支援 `--case-label`；R8 已依 SOP 重跑同步。
- 新增 `scripts/verify_site.py`，檢查 HTML、canonical、JSON-LD、本地連結/資產、sitemap、blog/cases/home 資料一致性、聯絡資訊與救援工具無儲存/網路 API。
- 新增 `/rescue-request`「救援需求快速整理」純前端頁：手機表單、友善驗證、去識別化提醒、可複製訊息、導向官方 LINE；首頁、服務範圍與 vCard 均有入口，sitemap 已加入。
- 更新首頁：最新案例改為可鍵盤操作的 anchor 卡片，加入快速整理 CTA，修正 WebPage schema，移除/弱化無法驗證的時效、價格與敏感技術宣稱。
- 重寫 `WEBSITE_PUBLISH_SOP.md` 為安全發布流程；更新 AI SOP，明確記錄 GBP API 停用、禁止 force push/隱藏 SEO/未授權外部公開。
- vCard OG 預覽確認使用 1200×630 名片式 `img/vcard-preview.jpg`，不是單獨 QR code。
- 驗證結果：`SITE_VERIFY_OK`（174 HTML）、Python compile、JS syntax、`git diff --check`、Playwright 手機/桌面 smoke test、救援表單產生/複製/LINE/隱私阻擋測試均通過。
- 目前執行環境無法連線 `carkey.com.tw` 或 GitHub（proxy 502 / ERR_FAILED）；遠端推送與正式站複驗需以實際 push 結果為準，不得宣稱已部署。
- 服務區域頁新增純前端行政區查詢：依官網已列 10 區、主要縣市未列區、其他未列區提供保守分級與下一步，不承諾到場/時效；攔截完整地址、車牌與 VIN，提醒勿填姓名。
- 查詢結果可將安全的行政區參數帶入 `/rescue-request`；救援整理頁只讀取一次 URL 參數預填，不儲存、不自動傳送。手機瀏覽器已測試空白、已列、未列與隱私攔截流程。
- 依使用者追加需求，`rescue-request.html` 新增純前端「草稿留存／轉交」：僅以 URL `#draft=` 片段保存安全欄位，支援部分填寫、重載還原、產生/更新、複製、清除（保留表單）與返回編輯；不使用 query 作為草稿、storage/cookie/後端/短網址/外部 API。備註預設排除，勾選並再次確認去識別化後才納入；載入時採版本、長度、選項與敏感格式白名單檢查。
- 最終整合提交訊息為 `feat: repair publishing flow and add rescue request tools`；GitHub HTTPS 經執行環境代理持續 502／逾時，截止前尚未能確認遠端推送成功。
