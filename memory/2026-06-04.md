- Integrated concurrent remote updates without force-pushing. Kept the rescue intake entirely client-side with no storage/backend/external API, localised the LINE QR asset, and hardened Blogger scripts to require explicit --publish plus --approved.
- 服務範圍頁新增純前端縣市選擇與區域輸入：即時分成已列、主要縣市未指定區、未列區域，所有結果提醒僅填大概區域且未確認不保證到府；CTA 以 `?area=` 帶入 `/rescue-request`。未接地圖、GBP、後端或外部 API。
