# 2026-04-29

## Google Business Profile OAuth

- Google Business Profile OAuth client JSON 已放在 `secrets/google-business-profile-client.json`，`secrets/` 已被 Git 忽略。
- 成功完成 OAuth 授權，token 儲存在 `secrets/google-business-profile-token.json`。
- 授權 scope：`https://www.googleapis.com/auth/business.manage`。
- 查詢商家帳號時遇到 403 `SERVICE_DISABLED`：需要在同一個 Google Cloud 專案啟用 `My Business Account Management API`，後續還需要 `My Business Business Information API` 與 `Google My Business API`。
- Google 商家工具位於 `scripts/google_business_profile/`，公開發文必須加 `--publish --approved`，避免誤發。
