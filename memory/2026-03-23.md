## 2026-03-23

- **Website Publish SOP review**: Boss Otto reviewed the `WEBSITE_PUBLISH_SOP.md` process for updating `carkey.com.tw` (which involves writing articles with specific styles, updating `blog.json`, `cases.html`, `index.html`, and pushing to GitHub).
- **New Project: Blogger Automation**: Otto created a new Blogger site: `https://signal-noise-decoder.blogspot.com/`. He requested an automated schedule to repurpose past case studies into fresh SEO articles on Blogger every day at 10 PM.
- **Progress on Blogger Automation**:
    - Chose the **Google Blogger API** method over email posting.
    - Set up a GCP Project (`helical-ascent-491017-q6`, app name: `ProCore-Blogger-Bot`) for OAuth access.
    - Generated Desktop App OAuth Credentials and saved the JSON to `scripts/blogger/client_secret.json`.
    - Wrote `scripts/blogger/auth_flow.py` to trigger the local browser login for the initial token.
    - Encountered a `403 access_denied` error during login because the email `sig.noise.decoder@gmail.com` wasn't added as a "Test User" in the new GCP Console UI (now found under the "Audience" / "目標對象" tab instead of the main OAuth consent screen page).
- **Pending**: Waiting for Otto to add his email to the test users list so we can run `auth_flow.py` again, obtain `token.json`, and then write the cron job.