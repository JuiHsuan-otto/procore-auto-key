# 2026-03-19 Durable Memories

## 案件處理紀錄
- **2018 Porsche Cayenne (彰化和美)**: 完成新增智慧鑰匙 SEO 文章發布。
    - 圖片存儲路徑: `img/porsche-cayenne-hemei/` (case-1.jpg, case-2.jpg)。
    - 文章路徑: `article-porsche-cayenne-hemei.html`。
    - 已同步更新: `blog.json`, `cases.html`, `index.html`。
    - 已推送到 GitHub 儲存庫 `procore-auto-key` 的 `main` 與 `production` 分支。

## 網站維護 SOP
- **質感要求**: 頁面必須符合「黑金質感」設計，使用 Tailwind CSS, background `#050505`, gold accent `#D4AF37`, 及 `glass-panel` 效果。參考範本: `article-benz-glk-miaoli-akl.html`。
- **發布清單**: 每次發文必須同步更新：
    1. 生成 `article-*.html` 並放入 `img/` 圖片。
    2. 更新 `blog.json` 索引。
    3. 更新 `cases.html` 的 `caseData` 陣列（實績展示頁面）。
    4. 更新 `index.html` 的 `latestCases` 陣列（首頁最新實績區塊）。
    5. 同步推送至 GitHub 分支（`main` 及 `production`）。

## 環境設定
- 官網 GitHub Repo: `https://github.com/JuiHsuan-otto/procore-auto-key.git`。
- Vercel 自動部署已連動，Push 後約 1-2 分鐘生效。
